---
phase: 12-provisioning-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/models/node.py
  - inference_proxy/provisioning/state.py
  - inference_proxy/config/settings.py
  - inference_proxy/resilience/health_checker.py
  - tests/models/test_node.py
  - tests/provisioning/test_state.py
  - tests/resilience/test_health_checker.py
autonomous: true
requirements: [PROV-06, PROV-07]

must_haves:
  truths:
    - "NodeStatus.PROVISIONING exists and equals 'provisioning'"
    - "ProvisioningStep enum has 13 members matching D-06 step sequence"
    - "ProvisioningState model is frozen and has hostname, current_step, started_at, updated_at, failed_step, error fields"
    - "Health checker skips nodes with PROVISIONING status"
    - "ProvisioningSettings has min_disk_gb defaulting to 20"
  artifacts:
    - path: "inference_proxy/provisioning/state.py"
      provides: "ProvisioningStep enum, ProvisioningState model"
      contains: "class ProvisioningStep"
    - path: "inference_proxy/models/node.py"
      provides: "PROVISIONING variant on NodeStatus"
      contains: "PROVISIONING"
    - path: "inference_proxy/config/settings.py"
      provides: "min_disk_gb on ProvisioningSettings"
      contains: "min_disk_gb"
    - path: "inference_proxy/resilience/health_checker.py"
      provides: "PROVISIONING guard in probe loop"
      contains: "PROVISIONING"
  key_links:
    - from: "inference_proxy/resilience/health_checker.py"
      to: "inference_proxy/models/node.py"
      via: "NodeStatus.PROVISIONING check"
      pattern: "NodeStatus\\.PROVISIONING"
    - from: "inference_proxy/provisioning/state.py"
      to: "inference_proxy/models/node.py"
      via: "Follows same StrEnum + frozen BaseModel pattern"
      pattern: "StrEnum"
---

<objective>
Create the foundational types for provisioning robustness and wire the health checker guard.

Purpose: PROV-06 needs a ProvisioningStep enum and ProvisioningState model before the provisioner can track state. PROV-07 needs NodeStatus.PROVISIONING and a health checker guard before the provisioner can register nodes with that status. These are prerequisites for Plan 02.

Output: state.py with ProvisioningStep + ProvisioningState, PROVISIONING on NodeStatus, min_disk_gb on settings, health checker skip guard, and tests for all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-provisioning-robustness/12-CONTEXT.md
@.planning/phases/12-provisioning-robustness/12-RESEARCH.md
@.planning/phases/12-provisioning-robustness/12-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From inference_proxy/models/node.py:
```python
class NodeStatus(StrEnum):
    HEALTHY = "healthy"
    UNHEALTHY = "unhealthy"
    DRAINING = "draining"
    UNKNOWN = "unknown"

class Node(BaseModel):
    model_config = ConfigDict(frozen=True)
    node_id: str
    endpoint: str
    status: NodeStatus = NodeStatus.UNKNOWN
    model: str = ""
    last_heartbeat: datetime | None = None
    capabilities: NodeCapabilities = Field(default_factory=NodeCapabilities)
    active_connections: int = 0
```

From inference_proxy/config/settings.py:
```python
class ProvisioningSettings(BaseModel):
    health_poll_timeout: int = 600
    health_poll_interval: int = 10
    vllm_port: int = 8000
```

From inference_proxy/resilience/health_checker.py:
```python
def _probe_all_nodes(registry, circuit_breaker_registry, client, consecutive_failures, failure_threshold):
    nodes = registry.get_all()
    for node in nodes:
        _probe_node(node_id=node.node_id, endpoint=node.endpoint, current_status=node.status, ...)
```

From tests/models/test_node.py line 19:
```python
assert len(NodeStatus) == 4  # Must become 5
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProvisioningStep enum, ProvisioningState model, NodeStatus.PROVISIONING, min_disk_gb setting</name>
  <files>
    inference_proxy/provisioning/state.py,
    inference_proxy/models/node.py,
    inference_proxy/config/settings.py,
    tests/provisioning/test_state.py,
    tests/models/test_node.py
  </files>
  <read_first>
    inference_proxy/models/node.py,
    inference_proxy/config/settings.py,
    tests/models/test_node.py
  </read_first>
  <behavior>
    - ProvisioningStep has 13 members: PENDING, PREFLIGHT, NVIDIA_REPO, SYSTEM_UPDATE, NVIDIA_DRIVER, NVIDIA_CDI, NFS_MOUNT, FIREWALL, STARTING_VLLM, HEALTH_POLL, REGISTERING, COMPLETE, FAILED
    - ProvisioningState is frozen, has fields: hostname (str), current_step (ProvisioningStep), started_at (datetime), updated_at (datetime), failed_step (str | None = None), error (str | None = None)
    - ProvisioningState round-trips through model_dump(mode="json") and model_validate
    - ProvisioningState rejects mutation (frozen)
    - NodeStatus.PROVISIONING == "provisioning" and len(NodeStatus) == 5
    - ProvisioningSettings.min_disk_gb defaults to 20
  </behavior>
  <action>
    Create inference_proxy/provisioning/state.py with ProvisioningStep(StrEnum) and ProvisioningState(BaseModel, frozen=True) per D-06, D-07, D-08. Follow the exact patterns from inference_proxy/models/node.py: from __future__ import annotations, StrEnum with lowercase values, frozen BaseModel with ConfigDict(frozen=True).

    ProvisioningStep members per D-06: PENDING="pending", PREFLIGHT="preflight", NVIDIA_REPO="nvidia_repo", SYSTEM_UPDATE="system_update", NVIDIA_DRIVER="nvidia_driver", NVIDIA_CDI="nvidia_cdi", NFS_MOUNT="nfs_mount", FIREWALL="firewall", STARTING_VLLM="starting_vllm", HEALTH_POLL="health_poll", REGISTERING="registering", COMPLETE="complete", FAILED="failed".

    ProvisioningState fields per D-07, D-08: hostname (str), current_step (ProvisioningStep), started_at (datetime), updated_at (datetime), failed_step (str | None = None), error (str | None = None).

    Add PROVISIONING = "provisioning" to NodeStatus enum in node.py per D-09.

    Add min_disk_gb: int = 20 to ProvisioningSettings in settings.py per D-02.

    Update tests/models/test_node.py: change len(NodeStatus) == 4 to == 5, add assert NodeStatus.PROVISIONING == "provisioning".

    Create tests/provisioning/test_state.py with tests for enum member count (13), ProvisioningState round-trip, frozen mutation rejection, and FAILED state with failed_step + error per D-08.
  </action>
  <verify>
    <automated>cd /home/grafuls/Sources/inference-proxy && python -m pytest tests/provisioning/test_state.py tests/models/test_node.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/provisioning/state.py contains class ProvisioningStep(StrEnum) with 13 members
    - inference_proxy/provisioning/state.py contains class ProvisioningState(BaseModel) with ConfigDict(frozen=True)
    - ProvisioningState has fields: hostname, current_step, started_at, updated_at, failed_step, error
    - inference_proxy/models/node.py contains PROVISIONING = "provisioning"
    - inference_proxy/config/settings.py contains min_disk_gb: int = 20 in ProvisioningSettings
    - tests/models/test_node.py asserts len(NodeStatus) == 5
    - tests/provisioning/test_state.py exits 0
    - tests/models/test_node.py exits 0
  </acceptance_criteria>
  <done>ProvisioningStep enum has 13 members, ProvisioningState model is frozen with all 6 fields, NodeStatus has PROVISIONING variant, ProvisioningSettings has min_disk_gb=20, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Health checker skips PROVISIONING nodes</name>
  <files>
    inference_proxy/resilience/health_checker.py,
    tests/resilience/test_health_checker.py
  </files>
  <read_first>
    inference_proxy/resilience/health_checker.py,
    tests/resilience/test_health_checker.py
  </read_first>
  <behavior>
    - A node with status=PROVISIONING is not probed by the health checker
    - A node with status=PROVISIONING is not marked UNHEALTHY even after multiple probe cycles
    - Health checker still probes HEALTHY and UNHEALTHY nodes normally when PROVISIONING nodes are present
  </behavior>
  <action>
    Add a guard in _probe_all_nodes() in health_checker.py per D-09: before the _probe_node() call inside the for-node loop, add "if node.status == NodeStatus.PROVISIONING: continue" with a structlog debug log "skipping_provisioning_node". The guard goes at the top of the for loop body, before _probe_node is called.

    Add TestProvisioningNodeSkipped class to tests/resilience/test_health_checker.py. Use the existing _make_node fixture with status=NodeStatus.PROVISIONING. Register one PROVISIONING node and one HEALTHY node. Run one probe cycle via the stop_after_one_iteration pattern already used in that file. Assert the PROVISIONING node's status is unchanged and the mock HTTP client was called exactly once (only for the HEALTHY node, not the PROVISIONING node).
  </action>
  <verify>
    <automated>cd /home/grafuls/Sources/inference-proxy && python -m pytest tests/resilience/test_health_checker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/resilience/health_checker.py contains "NodeStatus.PROVISIONING" in _probe_all_nodes
    - tests/resilience/test_health_checker.py contains class TestProvisioningNodeSkipped
    - A PROVISIONING node in the registry is not probed (mock_client.get called only for HEALTHY node)
    - tests/resilience/test_health_checker.py exits 0
    - python -m pytest tests/ -x -q exits 0 (full suite green)
  </acceptance_criteria>
  <done>Health checker skips PROVISIONING nodes, no spurious probes, full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| etcd state writes | Provisioning state written to etcd -- internal network, trusted |
| Node status enum | New PROVISIONING variant consumed by health checker, router |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ProvisioningState in etcd | accept | Internal network only, etcd access trusted per project constraints. No auth on etcd in v1. |
| T-12-02 | Denial of Service | NodeStatus.PROVISIONING | mitigate | Health checker guard prevents probe storms against nodes without vLLM running. Guard checks status per-node, not per-cycle, so mid-cycle additions are handled. |
| T-12-03 | Information Disclosure | ProvisioningState error field | accept | Error messages contain step names and SSH diagnostic output. Internal network only, no PII. |
</threat_model>

<verification>
```bash
cd /home/grafuls/Sources/inference-proxy && python -m pytest tests/provisioning/test_state.py tests/models/test_node.py tests/resilience/test_health_checker.py -x -q
```
</verification>

<success_criteria>
- ProvisioningStep enum has exactly 13 members matching D-06
- ProvisioningState model is frozen with 6 fields (D-07, D-08)
- NodeStatus.PROVISIONING exists and equals "provisioning" (D-09)
- ProvisioningSettings.min_disk_gb defaults to 20 (D-02)
- Health checker skips PROVISIONING nodes (D-09)
- All new and existing tests pass
</success_criteria>

<output>
Create `.planning/phases/12-provisioning-robustness/12-01-SUMMARY.md` when done
</output>
