---
phase: 09-live-metrics-and-auto-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/config/settings.py
  - inference_proxy/api/dashboard.py
  - inference_proxy/templates/dashboard.html
  - inference_proxy/static/js/dashboard.js
  - inference_proxy/static/css/dashboard.css
  - tests/config/test_settings.py
  - tests/api/test_dashboard.py
  - .env.example
autonomous: true
requirements:
  - METR-02
  - DASH-02

must_haves:
  truths:
    - "Dashboard node table has a Requests column showing per-node request counts"
    - "Dashboard auto-refreshes at a configurable polling interval without full page reload"
    - "Dashboard shows a Last updated timestamp that changes on each successful poll"
    - "On poll failure, stale data stays visible and a warning appears"
  artifacts:
    - path: "inference_proxy/config/settings.py"
      provides: "DashboardSettings sub-model with poll_interval"
      contains: "class DashboardSettings"
    - path: "inference_proxy/api/dashboard.py"
      provides: "Poll interval injected into template context"
      contains: "poll_interval"
    - path: "inference_proxy/templates/dashboard.html"
      provides: "Requests column header and POLL_INTERVAL_MS JS variable"
      contains: "Requests"
    - path: "inference_proxy/static/js/dashboard.js"
      provides: "Parallel-fetch polling loop with error resilience"
      contains: "setInterval"
  key_links:
    - from: "inference_proxy/config/settings.py"
      to: "inference_proxy/api/dashboard.py"
      via: "Depends(get_settings) injects settings.dashboard.poll_interval"
      pattern: "settings\\.dashboard\\.poll_interval"
    - from: "inference_proxy/api/dashboard.py"
      to: "inference_proxy/templates/dashboard.html"
      via: "Jinja2 template context dict"
      pattern: "poll_interval"
    - from: "inference_proxy/templates/dashboard.html"
      to: "inference_proxy/static/js/dashboard.js"
      via: "POLL_INTERVAL_MS global injected before script tag"
      pattern: "POLL_INTERVAL_MS"
    - from: "inference_proxy/static/js/dashboard.js"
      to: "/admin/nodes and /admin/metrics"
      via: "Promise.all parallel fetch"
      pattern: "fetch.*admin"
---

<objective>
Add per-node request counts to the dashboard node table and JS polling so the page stays current without manual refresh.

Purpose: Completes METR-02 (request counts visible on dashboard) and DASH-02 (auto-refresh via polling) -- the final two requirements for v1.1.
Output: Extended dashboard with 7-column node table, configurable polling loop, last-updated timestamp, and poll failure resilience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-live-metrics-and-auto-refresh/09-CONTEXT.md
@.planning/phases/09-live-metrics-and-auto-refresh/09-RESEARCH.md
@.planning/phases/09-live-metrics-and-auto-refresh/09-PATTERNS.md
@.planning/phases/09-live-metrics-and-auto-refresh/09-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From inference_proxy/config/settings.py (sub-model pattern):
  class GatewaySettings(BaseModel): host, port, graceful_shutdown_timeout
  Root Settings registers: gateway, etcd, routing, proxy, resilience, logging

From inference_proxy/api/dashboard.py (current route):
  @dashboard_router.get("/dashboard", response_class=HTMLResponse)
  async def dashboard(request: Request) -> HTMLResponse
  Uses: templates.TemplateResponse(request=request, name="dashboard.html")

From inference_proxy/config/dependencies.py:
  @lru_cache def get_settings() -> Settings
  (already exists, just needs to be imported in dashboard.py)

From inference_proxy/models/admin.py:
  AdminMetricsResponse: total_requests (int), per_model (dict[str,int]), per_node (dict[str,int])

From inference_proxy/api/admin.py:
  GET /admin/nodes -> list[AdminNodeResponse] (node_id, endpoint, model, status, active_connections, circuit_breaker_state)
  GET /admin/metrics -> AdminMetricsResponse

From inference_proxy/static/js/dashboard.js (current pattern):
  async function loadNodes() -- fetches /admin/nodes, builds table rows with 6 cells
  document.addEventListener("DOMContentLoaded", loadNodes)

From inference_proxy/templates/dashboard.html:
  6-column thead, tbody#node-table-body, colspan="6" on loading row
  Script tag: <script src="{{ url_for('static', path='js/dashboard.js') }}"></script>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend config, route injection, template, and CSS for metrics column and polling</name>
  <files>
    inference_proxy/config/settings.py,
    inference_proxy/api/dashboard.py,
    inference_proxy/templates/dashboard.html,
    inference_proxy/static/css/dashboard.css,
    .env.example
  </files>
  <read_first>
    inference_proxy/config/settings.py,
    inference_proxy/api/dashboard.py,
    inference_proxy/templates/dashboard.html,
    inference_proxy/static/css/dashboard.css,
    inference_proxy/config/dependencies.py,
    .env.example
  </read_first>
  <action>
    Settings (per D-03, D-04): Add DashboardSettings(BaseModel) with poll_interval: int = 10 and docstring "Dashboard UI configuration." Register as dashboard: DashboardSettings = DashboardSettings() on the root Settings class, after the logging field. Follow the exact sub-model pattern of GatewaySettings.

    Dashboard route (per D-03): Import Depends from fastapi, import get_settings from inference_proxy.config.dependencies, import Settings from inference_proxy.config.settings. Add settings: Settings = Depends(get_settings) parameter to the dashboard function. Pass poll_interval=settings.dashboard.poll_interval in the context dict of TemplateResponse.

    Template (per D-01, D-03, D-06, D-07): Add a 7th th with scope="col" and text "Requests" after the Circuit Breaker header. Update colspan="6" to colspan="7" on the loading td. Before the dashboard.js script tag, add an inline script block: const POLL_INTERVAL_MS = {{ poll_interval * 1000 }};. Add a p element with id="last-updated" below the closing table tag (inside main, before closing main). Add a p element with id="poll-warning" after last-updated, hidden by default (empty content). Per UI-SPEC: last-updated placement is below the table, above the footer.

    CSS: Add .last-updated class with font-size: 0.875rem and color: var(--text-light). Add .poll-warning class with font-size: 0.875rem and color: #ca8a04 (reuses existing warning amber). No other styles needed.

    .env.example: Add a Dashboard section with commented INFERENCE_PROXY_DASHBOARD__POLL_INTERVAL=10 following the existing section comment pattern.
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_dashboard.py tests/config/test_settings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - settings.py contains class DashboardSettings(BaseModel) with poll_interval: int = 10
    - settings.py root Settings class has field dashboard: DashboardSettings = DashboardSettings()
    - dashboard.py route signature includes settings: Settings = Depends(get_settings)
    - dashboard.py TemplateResponse context dict includes poll_interval key
    - dashboard.html thead has 7 th elements, the 7th being "Requests"
    - dashboard.html loading td has colspan="7"
    - dashboard.html contains script block with POLL_INTERVAL_MS = {{ poll_interval * 1000 }}
    - dashboard.html contains elements with id="last-updated" and id="poll-warning"
    - dashboard.css contains .last-updated and .poll-warning classes
    - .env.example contains INFERENCE_PROXY_DASHBOARD__POLL_INTERVAL
    - Existing tests pass: uv run pytest tests/api/test_dashboard.py tests/config/test_settings.py -x exits 0
  </acceptance_criteria>
  <done>Backend settings, route injection, template structure, and CSS all updated. Existing tests green. Dashboard HTML serves with 7-column header and POLL_INTERVAL_MS variable.</done>
</task>

<task type="auto">
  <name>Task 2: JS polling loop with parallel fetch, error resilience, and tests</name>
  <files>
    inference_proxy/static/js/dashboard.js,
    tests/api/test_dashboard.py,
    tests/config/test_settings.py
  </files>
  <read_first>
    inference_proxy/static/js/dashboard.js,
    inference_proxy/templates/dashboard.html,
    inference_proxy/api/admin.py,
    inference_proxy/models/admin.py,
    tests/api/test_dashboard.py,
    tests/config/test_settings.py,
    .planning/phases/09-live-metrics-and-auto-refresh/09-RESEARCH.md,
    .planning/phases/09-live-metrics-and-auto-refresh/09-PATTERNS.md
  </read_first>
  <action>
    Rewrite dashboard.js (per D-01, D-05, D-06, D-07):

    Replace loadNodes() with refreshDashboard(). Use Promise.all([fetch("/admin/nodes"), fetch("/admin/metrics")]) for parallel fetch. If either response is not ok, throw. Parse both JSON bodies. Build perNode from metrics.per_node (default to empty object if missing).

    Row building: Keep the existing 6-cell pattern (tdId, tdEndpoint, tdModel, tdStatus with badge, tdConn, tdCb with badge). Append a 7th cell tdReqs with textContent = perNode[node.node_id] || 0. Update all colspan="6" strings in JS to colspan="7" (empty state, error state).

    Timestamp (per D-06): After successful render, set document.getElementById("last-updated").textContent to "Last updated: " + new Date().toLocaleTimeString(). Add the CSS class last-updated to the element.

    Warning (per D-07): On fetch error in the catch block, set document.getElementById("poll-warning").textContent to "Update failed -- retrying..." and add class poll-warning. Do NOT clear the table body -- keep stale data visible. On success, clear the warning element textContent.

    Polling (per D-03, D-04): Replace the DOMContentLoaded listener. On DOMContentLoaded: call refreshDashboard() once, then setInterval(refreshDashboard, POLL_INTERVAL_MS). POLL_INTERVAL_MS is a global injected by the Jinja2 template script block from Task 1.

    Tests in test_settings.py: Add TestDefaultDashboardSettings asserting Settings(_env_file=None).dashboard.poll_interval == 10. Add TestEnvVarOverrideDashboardPollInterval using monkeypatch.setenv("INFERENCE_PROXY_DASHBOARD__POLL_INTERVAL", "30") and asserting settings.dashboard.poll_interval == 30. Add DashboardSettings to the TestSubModelsAreNotBaseSettings assertions. Import DashboardSettings in the import block.

    Tests in test_dashboard.py: Add TestDashboardRequestsColumn with test_contains_requests_column_header asserting "Requests" in response.text. Add TestDashboardPolling with test_contains_poll_interval_js_variable asserting "POLL_INTERVAL_MS" in response.text, and test_poll_interval_default_value asserting "10000" in response.text (10 seconds * 1000). Add test_contains_last_updated_element asserting 'id="last-updated"' in response.text. Add test_contains_poll_warning_element asserting 'id="poll-warning"' in response.text. Add test_contains_seven_column_headers updating the existing test or adding a parallel test that checks for all 7 headers including "Requests".
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_dashboard.py tests/config/test_settings.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.js contains async function refreshDashboard()
    - dashboard.js contains Promise.all with fetch("/admin/nodes") and fetch("/admin/metrics")
    - dashboard.js builds 7 cells per row, the 7th using perNode[node.node_id] || 0
    - dashboard.js contains setInterval(refreshDashboard, POLL_INTERVAL_MS)
    - dashboard.js updates element with id="last-updated" with toLocaleTimeString() on success
    - dashboard.js sets warning text on fetch error without clearing tbody
    - dashboard.js clears warning text on fetch success
    - All colspan values in dashboard.js are "7"
    - test_settings.py: TestDefaultDashboardSettings passes with poll_interval == 10
    - test_settings.py: TestEnvVarOverrideDashboardPollInterval passes with poll_interval == 30
    - test_dashboard.py: test_contains_requests_column_header passes
    - test_dashboard.py: test_contains_poll_interval_js_variable passes
    - test_dashboard.py: test_poll_interval_default_value passes (10000 in response text)
    - Full test suite: uv run pytest exits 0
  </acceptance_criteria>
  <done>Dashboard JS fetches nodes and metrics in parallel, renders 7-column table with request counts, polls at configurable interval, shows last-updated timestamp, handles errors gracefully. All new and existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /admin/* | Dashboard JS fetches admin endpoints; internal network only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Jinja2 poll_interval injection | mitigate | poll_interval is an int validated by Pydantic; injected in script block as numeric literal, no user input involved |
| T-09-02 | Information Disclosure | /admin/metrics per_node counts | accept | Counters are intentionally public on internal admin dashboard; no PII |
| T-09-03 | Denial of Service | setInterval polling | accept | 10s default interval on internal network; single browser tab per operator is expected use |
</threat_model>

<verification>
- uv run pytest tests/api/test_dashboard.py tests/config/test_settings.py -x -v
- uv run pytest (full suite green)
- uv run mypy inference_proxy/config/settings.py inference_proxy/api/dashboard.py
</verification>

<success_criteria>
- Dashboard at /dashboard renders 7-column node table with "Requests" column
- Request counts populated from /admin/metrics per_node data, defaulting to 0
- Page auto-refreshes via setInterval at POLL_INTERVAL_MS (default 10000ms)
- "Last updated: HH:MM:SS" timestamp visible below table, updates each poll cycle
- Poll failures show warning text without clearing stale data
- INFERENCE_PROXY_DASHBOARD__POLL_INTERVAL env var overrides default interval
- All tests pass including new settings and dashboard tests
</success_criteria>

<output>
Create `.planning/phases/09-live-metrics-and-auto-refresh/09-01-SUMMARY.md` when done
</output>
