---
phase: 14-dashboard-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/templates/dashboard.html
  - inference_proxy/static/css/dashboard.css
  - inference_proxy/static/js/dashboard.js
  - tests/api/test_dashboard.py
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Dashboard has a visible hostname input form above the node fleet table"
    - "Submitting a hostname calls POST /admin/nodes/setup and shows flash confirmation"
    - "Each node row has a Teardown button in an Actions column"
    - "Clicking Teardown shows window.confirm then calls DELETE /admin/nodes/{id}"
    - "A Provisioning Tasks panel below the table shows task status with step badges"
    - "Tasks panel polls on the same interval as the node table via shared refreshDashboard"
  artifacts:
    - path: "inference_proxy/templates/dashboard.html"
      provides: "Setup form, Actions column header, tasks panel section"
      contains: "setup-form"
    - path: "inference_proxy/static/css/dashboard.css"
      provides: "Badge classes for provisioning step states"
      contains: "badge-in-progress"
    - path: "inference_proxy/static/js/dashboard.js"
      provides: "Form handler, teardown handler, tasks renderer, extended Promise.all"
      contains: "renderTasks"
    - path: "tests/api/test_dashboard.py"
      provides: "Assertions for setup form, actions column, tasks panel, badge CSS"
      contains: "Actions"
  key_links:
    - from: "dashboard.js form handler"
      to: "POST /admin/nodes/setup"
      via: "fetch with JSON body"
      pattern: "fetch.*admin/nodes/setup"
    - from: "dashboard.js teardown handler"
      to: "DELETE /admin/nodes/{id}"
      via: "fetch with DELETE method"
      pattern: "fetch.*admin/nodes/"
    - from: "dashboard.js refreshDashboard"
      to: "GET /admin/provisioning/tasks"
      via: "Promise.all third fetch"
      pattern: "admin/provisioning/tasks"
    - from: "dashboard.js renderTasks"
      to: "dashboard.css badge classes"
      via: "stepBadgeClass helper"
      pattern: "badge-in-progress"
---

<objective>
Add setup form, teardown buttons, and provisioning tasks panel to the dashboard.

Purpose: Complete the operator workflow -- Phase 13 built the admin API, this phase exposes it in the existing Jinja2+vanilla JS dashboard so operators don't need curl.
Output: Extended dashboard.html, dashboard.js, dashboard.css, and test_dashboard.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-dashboard-operations/14-CONTEXT.md
@.planning/phases/14-dashboard-operations/14-RESEARCH.md
@.planning/phases/14-dashboard-operations/14-PATTERNS.md

<interfaces>
<!-- Admin API endpoints called from JS (Phase 13 output) -->

From inference_proxy/api/admin.py:
- POST /admin/nodes/setup — accepts {"hostname": str}, returns 202 {"task_id": str}
- GET /admin/provisioning/tasks — returns list of TaskStatusResponse
- DELETE /admin/nodes/{id}?force=bool — returns 202 {"task_id": str}, 404 if unknown

From inference_proxy/models/admin.py:
- TaskStatusResponse fields: hostname, current_step, started_at, updated_at, failed_step (nullable), error (nullable)

From inference_proxy/provisioning/state.py:
- ProvisioningStep values: pending, preflight, nvidia_repo, system_update, nvidia_driver, nvidia_cdi, nfs_mount, firewall, starting_vllm, health_poll, registering, draining, stopping_container, deregistering, teardown_complete, complete, failed
- Terminal states: complete, teardown_complete, failed

From inference_proxy/models/node.py:
- NodeStatus values: healthy, unhealthy, draining, provisioning, unknown
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML structure + CSS badge classes + tests</name>
  <files>inference_proxy/templates/dashboard.html, inference_proxy/static/css/dashboard.css, tests/api/test_dashboard.py</files>
  <read_first>
    - inference_proxy/templates/dashboard.html (current 41-line template)
    - inference_proxy/static/css/dashboard.css (current 43-line stylesheet)
    - tests/api/test_dashboard.py (current 142-line test file)
    - inference_proxy/provisioning/state.py (ProvisioningStep enum values for badge classes)
  </read_first>
  <action>
    Extend dashboard.html with three additions per D-01, D-05, D-09:

    1. **Setup form (D-01, D-02):** Add a form element with id="setup-form" above the existing table inside main. Contains one text input with id="setup-hostname" (placeholder "Hostname"), one submit button with id="setup-btn" (text "Setup"), and a span with id="setup-status" for the flash message (D-03). Form has method="post" but submit is handled by JS (preventDefault). Simple.css auto-styles form elements.

    2. **Actions column (D-09):** Add an 8th th element with scope="col" and text "Actions" after the "Requests" column in thead. Update the loading row colspan from "7" to "8".

    3. **Tasks panel (D-05):** Add a section element with id="tasks-panel" below the last-updated/poll-warning paragraphs but still inside main. Contains an h2 "Provisioning Tasks" and a table with thead (columns: Hostname, Current Step, Status, Started, Updated) and tbody with id="tasks-table-body" (default row: td colspan="5" text "No provisioning tasks").

    Extend dashboard.css with provisioning badge classes per RESEARCH badge color strategy:

    4. Add .badge-complete and .badge-teardown_complete to the existing green group (append to the .badge-healthy, .badge-closed rule).
    5. Add .badge-failed to the existing red group (append to the .badge-unhealthy, .badge-open rule).
    6. Add a new rule for .badge-in-progress with background-color #2563eb (blue) and color #fff.
    7. Add .badge-provisioning to a suitable group (blue for consistency with in-progress, since provisioning IS in-progress).

    Extend test_dashboard.py:

    8. Update TestDashboardTableStructure.test_contains_all_seven_column_headers: rename to test_contains_all_eight_column_headers, add "Actions" to the headers list, update docstring to say 8.
    9. Add TestSetupForm class with test methods: test_contains_setup_form (assert 'id="setup-form"' in response.text), test_contains_hostname_input (assert 'id="setup-hostname"' in response.text), test_contains_setup_button (assert 'id="setup-btn"' in response.text).
    10. Add TestTasksPanel class with test method: test_contains_tasks_panel (assert 'id="tasks-panel"' in response.text), test_contains_tasks_table_body (assert 'id="tasks-table-body"' in response.text).
    11. Add provisioning badge assertions to TestDashboardBadgeCSS: test_badge_css_contains_provisioning_classes asserting .badge-complete, .badge-failed, .badge-in-progress all exist in CSS file.
  </action>
  <verify>
    <automated>cd /home/grafuls/Sources/inference-proxy && uv run pytest tests/api/test_dashboard.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.html contains id="setup-form" with id="setup-hostname" input and id="setup-btn" button
    - dashboard.html contains th with text "Actions" as 8th column
    - dashboard.html loading row has colspan="8"
    - dashboard.html contains id="tasks-panel" section with id="tasks-table-body" tbody
    - dashboard.css contains .badge-complete, .badge-failed, .badge-in-progress selectors
    - test_dashboard.py has TestSetupForm class and TestTasksPanel class
    - test_dashboard.py column header test asserts 8 headers including "Actions"
    - uv run pytest tests/api/test_dashboard.py -x passes all tests
  </acceptance_criteria>
  <done>Dashboard HTML has setup form, 8-column table with Actions header, and tasks panel section. CSS has provisioning badge classes. All new and existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: JavaScript behavior -- form handler, teardown, tasks renderer</name>
  <files>inference_proxy/static/js/dashboard.js</files>
  <read_first>
    - inference_proxy/static/js/dashboard.js (current 83-line file)
    - inference_proxy/templates/dashboard.html (read after Task 1 for element IDs)
    - inference_proxy/models/admin.py (TaskStatusResponse fields)
    - inference_proxy/provisioning/state.py (ProvisioningStep terminal states)
  </read_first>
  <action>
    Extend dashboard.js with all dynamic behavior per D-03, D-04, D-06, D-08, D-09, D-10, D-11, D-12:

    1. **Extend refreshDashboard() Promise.all (D-08):** Add fetch("/admin/provisioning/tasks") as third element. Destructure as tasksResp. Handle tasksResp failure gracefully per RESEARCH pitfall 4: if tasksResp.ok is false, set tasks to empty array and continue (do not throw -- nodes and metrics should still render). Parse as tasksResp.json() when ok.

    2. **Actions column cell per node row (D-09, D-12):** Inside the existing for-of loop over nodes, after the tdReqs cell and before tbody.appendChild(tr), add a new td with a button element. Button text "Teardown", button type "button". Set button.disabled = true when node.status is "provisioning" or "draining" (D-12). Attach click handler inline using button.addEventListener("click", ...) that calls handleTeardown(node.node_id) (defined in step 4).

    3. **Update empty state colspan:** Change the existing innerHTML colspan="7" to colspan="8" in the nodes.length === 0 branch.

    4. **handleTeardown function (D-10, D-11):** Define async function handleTeardown(nodeId). Call window.confirm with message per D-10: "Teardown node {nodeId}? This will drain connections and stop the container." If cancelled, return. If confirmed, fetch DELETE /admin/nodes/{nodeId} (no force param -- D-11 says graceful only). On success, find the button that triggered the call via the event and disable it to prevent double-click per RESEARCH pitfall 2. On non-2xx response, alert the error (Claude's discretion for error display).

    5. **Setup form handler (D-03, D-04):** In the DOMContentLoaded listener, after the existing refreshDashboard + setInterval calls, get the form element by id="setup-form" and add a submit event listener. On submit: preventDefault. Get hostname from input id="setup-hostname", trim it. If empty, return (D-04 non-empty check). Disable the setup button (id="setup-btn"). POST to /admin/nodes/setup with JSON body {"hostname": hostname}. On success: set id="setup-status" textContent to "Setup started for {hostname}", clear the input value, setTimeout to re-enable button after 2000ms (D-03). On non-2xx: set setup-status textContent to error message, re-enable button immediately.

    6. **renderTasks function (D-05, D-06, D-07):** Define function renderTasks(tasks) that receives the parsed tasks array. Get tbody by id="tasks-table-body". If tasks is empty or undefined, set innerHTML to a single row with colspan="5" and text "No provisioning tasks" (Claude's discretion empty state). Otherwise, clear innerHTML and build rows with createElement: hostname td, current_step td with badge (use stepBadgeClass helper), status td showing "failed" if failed_step is non-null else "in progress" if non-terminal else the step value, started_at td formatted with toLocaleString, updated_at td formatted with toLocaleString. Use textContent for all user-provided values (XSS prevention).

    7. **stepBadgeClass helper:** Define function stepBadgeClass(step) that returns "badge-complete" for "complete" or "teardown_complete", "badge-failed" for "failed", and "badge-in-progress" for everything else. Used by renderTasks to set badge className to "badge " + stepBadgeClass(step).

    8. **Call renderTasks from refreshDashboard:** After the existing node table rendering (after the for-of loop and the else/if branches), call renderTasks(tasks) where tasks is the parsed array from step 1 (or empty array if fetch failed).
  </action>
  <verify>
    <automated>cd /home/grafuls/Sources/inference-proxy && uv run pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.js contains fetch("/admin/provisioning/tasks") inside Promise.all
    - dashboard.js contains function renderTasks
    - dashboard.js contains function stepBadgeClass
    - dashboard.js contains function handleTeardown with window.confirm call
    - dashboard.js form submit handler references id="setup-form" and calls fetch POST /admin/nodes/setup
    - dashboard.js Teardown button disabled check uses ["provisioning", "draining"].includes
    - dashboard.js empty state colspan is "8"
    - dashboard.js uses textContent (not innerHTML) for all user-provided values
    - Full test suite passes: uv run pytest tests/ -x
  </acceptance_criteria>
  <done>Dashboard JS extends refreshDashboard with tasks fetch, renders tasks panel with step badges, handles setup form submission with 2s disable + flash, handles teardown with confirm dialog, disables teardown for provisioning/draining nodes. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Operator input (hostname) crosses from client to backend via POST /admin/nodes/setup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | DOM rendering of hostname/step values | mitigate | Use textContent for all user-provided values; never innerHTML with dynamic data (established codebase pattern) |
| T-14-02 | Tampering | Double-submit on setup | mitigate | D-03 button disable for 2s + flash confirmation |
| T-14-03 | Tampering | Double-click on teardown | mitigate | Disable button immediately after DELETE succeeds; next poll rebuilds table with fresh state |
| T-14-04 | Denial of Service | Rapid polling | accept | Poll interval controlled by server-side setting (default 10s); internal network only |
</threat_model>

<verification>
1. GET /dashboard returns HTML containing setup form, 8-column table, and tasks panel
2. Badge CSS contains .badge-complete, .badge-failed, .badge-in-progress
3. Full test suite green: uv run pytest tests/ -x
</verification>

<success_criteria>
- Dashboard has setup form with hostname input above node table (DASH-01)
- Each node row has a Teardown button in Actions column (DASH-02)
- Tasks panel below table shows provisioning task status with step badges (DASH-03)
- All 12 locked decisions (D-01 through D-12) implemented
- Existing tests unchanged and passing; new tests covering HTML structure and CSS classes
</success_criteria>

<output>
Create `.planning/phases/14-dashboard-operations/14-01-SUMMARY.md` when done
</output>
