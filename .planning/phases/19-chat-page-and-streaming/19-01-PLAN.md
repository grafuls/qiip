---
phase: 19-chat-page-and-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/api/chat.py
  - inference_proxy/main.py
  - inference_proxy/templates/dashboard.html
  - inference_proxy/templates/chat.html
  - inference_proxy/static/css/chat.css
  - tests/api/test_chat.py
autonomous: true
requirements: [CHAT-01, CHAT-03]

must_haves:
  truths:
    - "D-01: Chat page has bubble styles for user (right) and assistant (left) alignment"
    - "D-02: Input bar fixed to viewport bottom"
    - "D-03: Dashboard nav bar has a Chat link pointing to /chat"
    - "D-04: Chat page loads marked.js CDN script for markdown rendering"
    - "D-05: Chat page has a model selector dropdown element"
    - "GET /chat returns 200 with text/html content"
    - "Chat page has a textarea and send button"
    - "Chat page loads dashboard.css, chat.css, and chat.js"
  artifacts:
    - path: "inference_proxy/api/chat.py"
      provides: "chat_router with GET /chat"
      exports: ["chat_router"]
    - path: "inference_proxy/templates/chat.html"
      provides: "Chat page HTML shell"
      contains: "model-selector"
    - path: "inference_proxy/static/css/chat.css"
      provides: "Chat-specific styles (bubbles, input bar, message area)"
      min_lines: 50
    - path: "tests/api/test_chat.py"
      provides: "Route and template integration tests"
      min_lines: 30
  key_links:
    - from: "inference_proxy/main.py"
      to: "inference_proxy/api/chat.py"
      via: "include_router(chat_router)"
      pattern: "chat_router"
    - from: "inference_proxy/templates/chat.html"
      to: "inference_proxy/static/css/chat.css"
      via: "url_for static link"
      pattern: "chat\\.css"
    - from: "inference_proxy/templates/dashboard.html"
      to: "/chat"
      via: "nav bar link"
      pattern: "href=.*chat"
---

<objective>
Create the chat page structure: FastAPI route, Jinja2 template, CSS styles, nav link, and integration tests.

Purpose: Deliver the static shell (all DOM elements, styles, and server wiring) that Plan 02's chat.js will bring to life. After this plan, GET /chat renders a styled chat page with all UI elements present but inert.

Output: chat.py, chat.html, chat.css, test_chat.py, modified main.py and dashboard.html
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-chat-page-and-streaming/19-CONTEXT.md
@.planning/phases/19-chat-page-and-streaming/19-RESEARCH.md
@.planning/phases/19-chat-page-and-streaming/19-PATTERNS.md
@.planning/phases/19-chat-page-and-streaming/19-UI-SPEC.md

<interfaces>
<!-- Existing code the executor needs -->

From inference_proxy/api/dashboard.py:
- chat.py mirrors this file exactly: APIRouter, Jinja2Templates, Path-based BASE_DIR, single GET handler returning TemplateResponse
- chat_router does NOT need Depends(get_settings) -- no server-side config to pass

From inference_proxy/main.py (lines 28-29, 259-261):
- Import: `from inference_proxy.api.dashboard import dashboard_router`
- Mount: `application.include_router(dashboard_router)` at line 261
- Add chat_router import + mount in the same pattern

From inference_proxy/templates/dashboard.html (lines 13-21):
- Nav bar: `<nav class="top-bar">` with `<div class="brand">` and `<button class="theme-toggle">`
- Add nav links between brand div and theme toggle button
- Both dashboard.html and chat.html need the Chat link

From tests/api/test_dashboard.py:
- Uses `client` fixture from tests/conftest.py
- Pattern: TestClass per concern, assertions on response.status_code, response.headers, response.text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat router and wire into application</name>
  <files>inference_proxy/api/chat.py, inference_proxy/main.py, inference_proxy/templates/dashboard.html</files>
  <read_first>
    inference_proxy/api/dashboard.py
    inference_proxy/main.py
    inference_proxy/templates/dashboard.html
  </read_first>
  <action>
    Create inference_proxy/api/chat.py following the dashboard.py pattern exactly per D-03. Define chat_router as APIRouter(tags=["chat"]) with a single GET /chat handler that returns TemplateResponse for "chat.html". No Depends(get_settings) needed -- no server-side context to pass. Use the same BASE_DIR / templates pattern from dashboard.py.

    In inference_proxy/main.py: add import `from inference_proxy.api.chat import chat_router` alongside the existing dashboard_router import (line 29 area). Add `application.include_router(chat_router)` after the dashboard_router mount (line 261 area).

    In inference_proxy/templates/dashboard.html: add a nav links section between the brand div (closing tag around line 19) and the theme-toggle button (line 20). Insert an anchor element with href="/chat", text "Chat", styled with a class nav-link. Also add an anchor for href="/dashboard" with text "Dashboard" so both pages have consistent navigation per D-03.
  </action>
  <verify>
    <automated>uv run python -c "from inference_proxy.api.chat import chat_router; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/api/chat.py exists and exports chat_router
    - chat_router has a GET /chat route returning HTMLResponse
    - main.py imports chat_router and calls include_router(chat_router)
    - dashboard.html nav bar contains href="/chat" with text "Chat"
  </acceptance_criteria>
  <done>chat_router created, mounted in app, dashboard nav bar has Chat link</done>
</task>

<task type="auto">
  <name>Task 2: Create chat page template and styles</name>
  <files>inference_proxy/templates/chat.html, inference_proxy/static/css/chat.css</files>
  <read_first>
    inference_proxy/templates/dashboard.html
    inference_proxy/static/css/dashboard.css
    .planning/phases/19-chat-page-and-streaming/19-UI-SPEC.md
    .planning/phases/19-chat-page-and-streaming/19-PATTERNS.md
  </read_first>
  <action>
    Create inference_proxy/templates/chat.html following the dashboard.html head pattern: same doctype, charset, viewport, Google Fonts links, same theme-init inline script. Load dashboard.css first (for base styles + theme vars), then chat.css. Include marked.js via CDN script tag (https://cdn.jsdelivr.net/npm/marked@18/lib/marked.umd.min.js) before chat.js per D-04.

    Page structure per UI-SPEC layout contract:
    - Nav bar: identical to dashboard.html nav bar including the Chat and Dashboard nav links added in Task 1, plus theme toggle. Title "QUADS -- Chat" in the HTML title element.
    - Model selector bar: a div below the nav bar containing a select element with id="model-select". Default option text "Loading models..." per D-05.
    - Message area: a div with id="message-area", role="log", aria-live="polite" for accessibility. Contains a centered empty-state div with heading "Start a conversation" and body "Select a model above and type a message to begin." per UI-SPEC copywriting contract.
    - Input bar: a div fixed to viewport bottom per D-02. Contains a textarea with id="chat-input", placeholder "Type a message...", and a button with id="send-btn" text "Send". Include a toast-container div with id="toast-container".
    - Load chat.js as last script before closing body.

    Create inference_proxy/static/css/chat.css with chat-specific styles. Do NOT redeclare CSS custom properties already in dashboard.css -- use var(--primary), var(--surface), var(--bg), var(--text), var(--border), var(--border-strong), var(--radius), var(--radius-lg), var(--danger).

    Chat.css rules per UI-SPEC dimensions:
    - .chat-page: full viewport height layout using display:flex, flex-direction:column, height:100vh (or calc(100vh - 56px) below nav bar)
    - .model-selector-bar: height 48px, padding 0 32px, background var(--surface), border-bottom 1px solid var(--border), display:flex, align-items:center
    - .message-area: flex:1, overflow-y:auto, padding 0 32px. Inner content max-width 768px centered.
    - .message-bubble: max-width 80%, padding 12px 16px, border-radius var(--radius-lg)
    - .bubble-user: margin-left:auto (right-aligned), background var(--primary), color white per D-01
    - .bubble-assistant: margin-right:auto (left-aligned), background var(--surface) per D-01
    - .input-bar: border-top 1px solid var(--border), background var(--surface), padding 16px 32px, display:flex, gap 8px, align-items:flex-end
    - #chat-input textarea: min-height 40px, max-height 200px, flex:1, border 1px solid var(--border-strong), border-radius var(--radius), font Open Sans 14px, resize:none
    - #send-btn: 40x40px, border-radius var(--radius), background var(--primary), color white, cursor pointer
    - .empty-state: centered text, color var(--text-muted) or var(--text) at 50% opacity
    - Markdown code blocks: background var(--bg), padding 12px 16px, border-radius var(--radius), overflow-x auto, font IBM Plex Mono 13px per D-04
    - Inline code: background var(--bg), padding 2px 6px, border-radius 4px
    - .bubble-error: color var(--danger) for inline error display per D-09
    - .streaming-cursor: blinking | indicator per UI-SPEC states
    - Dark theme: bubble-user uses var(--primary) with dark text color #111827 per UI-SPEC bubble colors
    - Responsive at 768px breakpoint: padding reduces to 16px, bubble max-width 90%
    - prefers-reduced-motion: disable streaming cursor animation
    - Nav link styling: .nav-link with appropriate color/hover matching top-bar aesthetic
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_chat.py -x 2>/dev/null || uv run python -c "from inference_proxy.main import create_app; app = create_app(); print('app ok')"</automated>
  </verify>
  <acceptance_criteria>
    - chat.html exists and contains: id="model-select", id="message-area", id="chat-input", id="send-btn", id="toast-container"
    - chat.html loads dashboard.css via url_for, chat.css via url_for, marked.js via CDN script, chat.js via url_for
    - chat.html has role="log" and aria-live="polite" on the message area
    - chat.html contains empty state text "Start a conversation"
    - chat.css exists with rules for .chat-page, .model-selector-bar, .message-area, .message-bubble, .bubble-user, .bubble-assistant, .input-bar, .streaming-cursor, .bubble-error
    - chat.css does NOT contain :root or [data-theme="dark"] custom property declarations that duplicate dashboard.css
    - chat.css contains @media (max-width: 768px) responsive rules
    - chat.css contains @media (prefers-reduced-motion: reduce) rule
  </acceptance_criteria>
  <done>Chat page template renders with full layout structure and all styled elements visible in both light and dark themes</done>
</task>

<task type="auto">
  <name>Task 3: Create chat route integration tests</name>
  <files>tests/api/test_chat.py</files>
  <read_first>
    tests/api/test_dashboard.py
    tests/conftest.py
    inference_proxy/templates/chat.html
  </read_first>
  <action>
    Create tests/api/test_chat.py mirroring the test_dashboard.py pattern. Use the existing client fixture from conftest.py (no new fixtures needed since chat_router is mounted in create_app).

    Test classes:

    TestChatRoute (CHAT-01): test GET /chat returns 200, test response content-type contains text/html, test chat served by same app as /v1/models (proves wiring).

    TestChatTemplate (CHAT-01, CHAT-02, CHAT-03): test HTML contains chat.css link, test HTML contains chat.js script, test HTML contains marked CDN script (string "marked"), test HTML contains model-select element (id="model-select") for CHAT-03, test HTML contains message-area element (id="message-area"), test HTML contains chat-input textarea (id="chat-input") for CHAT-01, test HTML contains send-btn button (id="send-btn") for CHAT-01, test HTML contains role="log" accessibility attribute, test HTML contains aria-live="polite" accessibility attribute.

    TestChatNavigation (D-03): test dashboard.html contains href="/chat", test chat.html contains href="/dashboard".

    Follow existing conventions: from __future__ import annotations, from pathlib import Path, from fastapi.testclient import TestClient. Class-based tests with descriptive docstrings referencing requirement IDs.
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_chat.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_chat.py exists with TestChatRoute, TestChatTemplate, TestChatNavigation classes
    - All tests pass with exit code 0
    - Tests assert: status 200, text/html content type, chat.css, chat.js, marked, model-select, message-area, chat-input, send-btn, role="log", aria-live="polite"
    - TestChatNavigation verifies Chat link on dashboard and Dashboard link on chat page
  </acceptance_criteria>
  <done>All chat route and template tests pass green, covering CHAT-01, CHAT-02, CHAT-03 HTML structure</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to /chat | Unauthenticated GET, returns static HTML (internal network only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | GET /chat | accept | Page contains no secrets -- static HTML shell. Internal network only |
| T-19-02 | Tampering | Nav bar links | accept | Links are hardcoded hrefs, no user input involved |
</threat_model>

<verification>
uv run pytest tests/api/test_chat.py -x -v
uv run pytest -x  # full suite still green
</verification>

<success_criteria>
- GET /chat returns 200 with a fully structured chat page
- Dashboard nav bar has a clickable Chat link
- Chat page has model selector, message area, textarea, send button
- All new tests pass, full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/19-chat-page-and-streaming/19-01-SUMMARY.md` when done
</output>
