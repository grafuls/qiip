---
phase: 25-core-models-and-runner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/models/llmfit.py
  - inference_proxy/llmfit/__init__.py
  - inference_proxy/llmfit/errors.py
  - inference_proxy/provisioning/ssh_client.py
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03

must_haves:
  truths:
    - "Pydantic models parse valid llmfit JSON into SystemInfo and ModelRecommendation objects"
    - "Extra fields in llmfit JSON are silently ignored (forward compatibility)"
    - "LLMFitError is the base exception; LLMFitTimeoutError and LLMFitParseError are subclasses"
    - "LLMFitParseError stores the raw stdout that failed to parse"
    - "D-01: SSHClient.run() executes a command and returns (stdout, stderr, exit_status)"
    - "D-02: SSHClient.run() times out via asyncio.wait_for() after configurable duration"
    - "SSHClient.run() raises SSHConnectionError and RemoteCommandError identically to run_streaming()"
  artifacts:
    - path: "inference_proxy/models/llmfit.py"
      provides: "SystemInfo, ModelRecommendation, LLMFitResult Pydantic models"
      contains: "class LLMFitResult"
    - path: "inference_proxy/llmfit/errors.py"
      provides: "LLMFitError, LLMFitTimeoutError, LLMFitParseError"
      contains: "class LLMFitParseError"
    - path: "inference_proxy/llmfit/__init__.py"
      provides: "Package init"
    - path: "inference_proxy/provisioning/ssh_client.py"
      provides: "run() method on SSHClient"
      contains: "async def run"
  key_links:
    - from: "inference_proxy/llmfit/errors.py"
      to: "LLMFitError base class"
      via: "inheritance"
      pattern: "class LLMFitTimeoutError(LLMFitError)"
    - from: "inference_proxy/provisioning/ssh_client.py"
      to: "asyncio.wait_for"
      via: "timeout wrapper"
      pattern: "asyncio\\.wait_for"
---

<objective>
Create the data contracts and SSH execution method that the LLMFitRunner (Plan 02) builds on.

Purpose: Establish typed models for llmfit JSON output, a domain error hierarchy, and a capture-all SSH command method -- the foundation for remote llmfit execution.
Output: Pydantic models (SystemInfo, ModelRecommendation, LLMFitResult), error classes (LLMFitError hierarchy), and SSHClient.run() method.
</objective>

<execution_context>
@/home/grafuls/.claude/get-shit-done/workflows/execute-plan.md
@/home/grafuls/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/grafuls/Sources/inference-proxy/.planning/ROADMAP.md
@/home/grafuls/Sources/inference-proxy/.planning/STATE.md
@/home/grafuls/Sources/inference-proxy/.planning/phases/25-core-models-and-runner/25-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From inference_proxy/models/quads.py (Pydantic model pattern):
```python
from pydantic import BaseModel, ConfigDict

class QUADSHost(BaseModel):
    model_config = ConfigDict(frozen=True)
    hostname: str
    gpu_vendor: str
```

From inference_proxy/provisioning/ssh_client.py (SSH error pattern):
```python
class SSHConnectionError(Exception):
    def __init__(self, host: str, reason: str) -> None:
        self.host = host
        self.reason = reason
        super().__init__(f"SSH connection to {host} failed: {reason}")

class RemoteCommandError(Exception):
    def __init__(self, host: str, command: str, exit_status: int, stderr: str = "") -> None:
        ...
```

From inference_proxy/provisioning/ssh_client.py (connection and error blocks to replicate):
```python
async with asyncssh.connect(
    host, username=self._username, client_keys=[str(self._key_path)],
    known_hosts=None, connect_timeout=self._connect_timeout,
) as conn:
    ...
except asyncssh.PermissionDenied as exc:
    raise SSHConnectionError(host, f"authentication failed: {exc}") from exc
except asyncssh.DisconnectError as exc:
    raise SSHConnectionError(host, f"disconnected: {exc.reason}") from exc
except OSError as exc:
    raise SSHConnectionError(host, str(exc)) from exc
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pydantic models and error hierarchy</name>
  <files>inference_proxy/models/llmfit.py, inference_proxy/llmfit/__init__.py, inference_proxy/llmfit/errors.py</files>
  <read_first>
    - inference_proxy/models/quads.py (frozen Pydantic model pattern)
    - inference_proxy/models/node.py (default-value pattern for optional fields)
    - inference_proxy/redfish/errors.py (domain error hierarchy pattern)
    - inference_proxy/provisioning/ssh_client.py lines 23-46 (structured error attributes pattern)
    - .planning/phases/25-core-models-and-runner/25-RESEARCH.md (llmfit JSON schema in Code Examples section)
    - .planning/phases/25-core-models-and-runner/25-PATTERNS.md (analog assignments)
  </read_first>
  <action>
    Create three files:

    1. inference_proxy/models/llmfit.py -- per D-03 (EXEC-03):
       Three frozen Pydantic models with ConfigDict(frozen=True, extra="ignore"):
       - SystemInfo: has_gpu (bool), gpu_vram_gb (float, default 0.0), gpu_name (str, default ""), cpu_name (str, default ""), total_ram_gb (float, default 0.0), backend (str, default ""). Include available_ram_gb (float, default 0.0), cpu_cores (int, default 0), unified_memory (bool, default False) from the JSON fixture.
       - ModelRecommendation: name (str), score (float, default 0.0), fit_level (str, default ""), estimated_tps (float, default 0.0), memory_required_gb (float, default 0.0), provider (str, default ""), best_quant (str, default ""), run_mode (str, default ""), params_b (float, default 0.0), context_length (int, default 0), utilization_pct (float, default 0.0), category (str, default ""), runtime (str, default "").
       - LLMFitResult: system (SystemInfo), models (list[ModelRecommendation]).
       Follow quads.py import pattern: from __future__ import annotations, from pydantic import BaseModel, ConfigDict.

    2. inference_proxy/llmfit/__init__.py -- empty file (matches redfish/__init__.py analog).

    3. inference_proxy/llmfit/errors.py -- per D-03, D-04:
       - LLMFitError(Exception): base class, no custom attributes needed. Simple pass body or minimal __init__.
       - LLMFitTimeoutError(LLMFitError): stores host (str) and timeout (float) as instance attributes. Message format: "llmfit timed out after {timeout}s on {host}".
       - LLMFitParseError(LLMFitError): stores reason (str) and raw_output (str) as instance attributes per D-04. Message format: "Failed to parse llmfit output: {reason}".
       Follow ssh_client.py structured error attributes pattern (store on self, call super().__init__ with formatted message).
       Use from __future__ import annotations.
  </action>
  <verify>
    <automated>python -c "from inference_proxy.models.llmfit import SystemInfo, ModelRecommendation, LLMFitResult; from inference_proxy.llmfit.errors import LLMFitError, LLMFitTimeoutError, LLMFitParseError; e = LLMFitParseError('bad', 'raw'); assert e.raw_output == 'raw'; assert issubclass(LLMFitTimeoutError, LLMFitError); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/models/llmfit.py contains class SystemInfo(BaseModel), class ModelRecommendation(BaseModel), class LLMFitResult(BaseModel)
    - All three model classes have ConfigDict(frozen=True, extra="ignore")
    - SystemInfo has has_gpu as required bool, all other fields have defaults
    - LLMFitResult.system is type SystemInfo, LLMFitResult.models is type list[ModelRecommendation]
    - inference_proxy/llmfit/errors.py contains class LLMFitError(Exception), class LLMFitTimeoutError(LLMFitError), class LLMFitParseError(LLMFitError)
    - LLMFitTimeoutError.__init__ accepts host and timeout, stores both as attributes
    - LLMFitParseError.__init__ accepts reason and raw_output, stores both as attributes (D-04)
    - inference_proxy/llmfit/__init__.py exists
    - python -c import of all classes succeeds
  </acceptance_criteria>
  <done>All three Pydantic models importable with frozen+extra=ignore config. Error hierarchy importable with LLMFitTimeoutError and LLMFitParseError storing structured context attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Add run() method to SSHClient</name>
  <files>inference_proxy/provisioning/ssh_client.py</files>
  <read_first>
    - inference_proxy/provisioning/ssh_client.py (full file -- the file being modified; run_streaming() lines 69-116 is the analog for connection + error handling blocks)
    - .planning/phases/25-core-models-and-runner/25-PATTERNS.md (connection block and error handling block to replicate exactly)
  </read_first>
  <action>
    Add a new async method run() to SSHClient, per D-01 and D-02.

    Signature: async def run(self, host: str, command: str, timeout: float = 60.0) -> tuple[str, str, int]
    Returns (stdout, stderr, exit_status).

    Implementation:
    - Open asyncssh.connect with identical parameters to run_streaming() (same username, client_keys, known_hosts=None, connect_timeout).
    - Inside the connection: call await asyncio.wait_for(conn.run(command), timeout=timeout) per D-02. Do NOT pass timeout to conn.run() -- asyncio.wait_for handles it.
    - On success: check result.exit_status; if non-zero raise RemoteCommandError (same pattern as run_streaming() lines 102-106, using result.stderr or "").
    - Return (result.stdout or "", result.stderr or "", result.exit_status if result.exit_status is not None else 0).
    - Replicate the exact same except block from run_streaming() for asyncssh.PermissionDenied, asyncssh.DisconnectError, OSError -> SSHConnectionError.
    - Do NOT catch asyncio.TimeoutError -- it bubbles to the caller (per D-03 and research).
    - Add import asyncio at the top of the file (needed for asyncio.wait_for).
    - Add structlog debug logging: log.debug("ssh_run_start", host=host, command=command) before connect and log.debug("ssh_run_complete", host=host, exit_status=...) after.

    Docstring: "Run *command* on *host*, return ``(stdout, stderr, exit_status)``. Timeout via ``asyncio.wait_for`` (D-02). Raises SSHConnectionError on auth/disconnect/OS errors. Raises RemoteCommandError on non-zero exit. asyncio.TimeoutError bubbles to caller."

    Place the method after run_streaming() and before upload().
  </action>
  <verify>
    <automated>python -c "import inspect; from inference_proxy.provisioning.ssh_client import SSHClient; sig = inspect.signature(SSHClient.run); params = list(sig.parameters); assert params == ['self', 'host', 'command', 'timeout'], f'Wrong params: {params}'; assert sig.parameters['timeout'].default == 60.0; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SSHClient has async def run(self, host: str, command: str, timeout: float = 60.0) -> tuple[str, str, int]
    - Method uses asyncio.wait_for(conn.run(command), timeout=timeout) per D-02
    - Method does NOT catch asyncio.TimeoutError (it bubbles)
    - Method raises RemoteCommandError on non-zero exit with same pattern as run_streaming()
    - Method has identical except block to run_streaming() for PermissionDenied/DisconnectError/OSError
    - import asyncio is present at top of file
    - python -c import and signature inspection succeeds
  </acceptance_criteria>
  <done>SSHClient.run() method exists with 60s default timeout, asyncio.wait_for wrapping, and error handling identical to run_streaming().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> remote host (SSH) | SSHClient.run() sends commands to remote servers |
| remote host -> gateway (stdout) | llmfit JSON output parsed by Pydantic models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | llmfit JSON output | mitigate | Pydantic model_validate with frozen=True and extra="ignore" -- rejects type mismatches, drops unknown fields, immutable after parse |
| T-25-02 | Tampering | SSH command string | accept | Command is hardcoded constant ("/usr/local/bin/llmfit recommend --json --force-runtime vllm") -- no user input interpolation. hostname is passed to asyncssh.connect as host parameter, not shell-interpolated |
| T-25-03 | Denial of Service | SSH hang | mitigate | asyncio.wait_for with 60s timeout per D-02 prevents indefinite hangs |
| T-25-04 | Information Disclosure | LLMFitParseError.raw_output | accept | raw_output stores llmfit stdout for debugging. Contains hardware specs and model names, not secrets. Phase 27 decides exposure policy |
</threat_model>

<verification>
- All new modules import cleanly: `python -c "from inference_proxy.models.llmfit import LLMFitResult; from inference_proxy.llmfit.errors import LLMFitError, LLMFitTimeoutError, LLMFitParseError"`
- SSHClient.run() method exists with correct signature
- Error hierarchy: LLMFitTimeoutError and LLMFitParseError are subclasses of LLMFitError
- LLMFitParseError stores raw_output attribute
- No new dependencies added to pyproject.toml
</verification>

<success_criteria>
- Pydantic models parse the llmfit JSON fixture from RESEARCH.md without errors
- Error classes store structured attributes (host/timeout on TimeoutError, reason/raw_output on ParseError)
- SSHClient.run() has identical error handling to run_streaming() (same except blocks)
- SSHClient.run() uses asyncio.wait_for() for timeout (not conn.run(timeout=N))
</success_criteria>

<output>
Create `.planning/phases/25-core-models-and-runner/25-01-SUMMARY.md` when done
</output>
