---
phase: 26-llmfit-installation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - auto-vllm/setup.sh
  - inference_proxy/provisioning/state.py
  - tests/provisioning/test_state.py
autonomous: true
requirements: [INST-01, INST-02]

must_haves:
  truths:
    - "setup.sh downloads and installs the llmfit binary to /usr/local/bin on target servers"
    - "llmfit installation failure does not block or fail the overall provisioning process"
    - "Successful installation is logged via [STEP:llmfit_install:OK]; failure is logged via [STEP:llmfit_install:WARN]"
    - "ProvisioningStep enum includes LLMFIT_INSTALL so the dashboard tracks the step"
  artifacts:
    - path: "auto-vllm/setup.sh"
      provides: "soft_step() wrapper, install_llmfit() function, LLMFIT_VERSION/LLMFIT_URL env vars"
      contains: "soft_step"
    - path: "inference_proxy/provisioning/state.py"
      provides: "LLMFIT_INSTALL enum member"
      contains: "LLMFIT_INSTALL"
    - path: "tests/provisioning/test_state.py"
      provides: "Updated member count and values assertions"
      contains: "LLMFIT_INSTALL"
  key_links:
    - from: "auto-vllm/setup.sh"
      to: "inference_proxy/provisioning/state.py"
      via: "step name llmfit_install must match ProvisioningStep.LLMFIT_INSTALL value"
      pattern: 'soft_step llmfit_install'
---

<objective>
Add llmfit binary installation to the provisioning setup script as a non-fatal step, and add the corresponding enum member so the dashboard tracks the step.

Purpose: After provisioning, target servers have `/usr/local/bin/llmfit` available for later model recommendation runs. If download fails (air-gap, rate limit), provisioning continues unblocked.

Output: Modified setup.sh with soft_step()/install_llmfit(), updated ProvisioningStep enum, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-llmfit-installation/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add soft_step wrapper, install_llmfit function, and invocation to setup.sh</name>
  <files>auto-vllm/setup.sh</files>
  <read_first>
    - auto-vllm/setup.sh (current script — env-var defaults at lines 5-9, step() at lines 12-21, idempotent functions at 24-98, main at 100-107)
  </read_first>
  <action>
Three additions to setup.sh, per D-02 and INST-01/INST-02:

1. Env-var defaults: After the VLLM_PORT line (line 9), add LLMFIT_VERSION and LLMFIT_URL variables following the NVIDIA_DRIVER_VERSION/NVIDIA_DRIVER_URL pattern. LLMFIT_VERSION defaults to 1.1.6. LLMFIT_URL defaults to https://github.com/AlexsJones/llmfit/releases/download/v${LLMFIT_VERSION}/llmfit-v${LLMFIT_VERSION}-x86_64-unknown-linux-musl.tar.gz

2. soft_step() wrapper: After the step() function (after line 21), add soft_step(). Same structure as step() — takes name + command args, emits [STEP:${name}:START] before, [STEP:${name}:OK] on success. Difference: on failure, emits [STEP:${name}:WARN] (non-fatal, continuing) and does NOT exit 1. Per D-02, existing step() stays unchanged.

3. install_llmfit() function: After configure_firewall() (after line 98), add install_llmfit(). Idempotent: if [ -x /usr/local/bin/llmfit ] then echo skip and return 0. Otherwise: wget -q the LLMFIT_URL to /tmp/llmfit.tar.gz, tar -xzf to /tmp/, sudo install -m 755 the binary found via find /tmp/ -name llmfit -type f -print -quit to /usr/local/bin/llmfit, then cleanup rm -rf /tmp/llmfit.tar.gz /tmp/llmfit-*. Uses find because tarball contains a nested directory.

4. Main section: After step firewall configure_firewall (line 105), add soft_step llmfit_install install_llmfit. This goes AFTER firewall (last critical step) and BEFORE the "Setup complete" echo. Uses soft_step (not step) so failure is non-fatal per INST-02.

The step name llmfit_install must exactly match the ProvisioningStep.LLMFIT_INSTALL value added in Task 2, because the provisioner does ProvisioningStep(step_name) from parsed [STEP:name:START] markers.
  </action>
  <acceptance_criteria>
    - setup.sh contains LLMFIT_VERSION="${LLMFIT_VERSION:-1.1.6}" after line 9
    - setup.sh contains LLMFIT_URL variable referencing github.com/AlexsJones/llmfit with ${LLMFIT_VERSION} interpolation
    - setup.sh contains a soft_step() function that emits [STEP:${name}:START], [STEP:${name}:OK], and [STEP:${name}:WARN] markers
    - soft_step() does NOT contain exit 1
    - step() function is unchanged from its current form
    - setup.sh contains install_llmfit() with idempotent check for /usr/local/bin/llmfit
    - install_llmfit() uses find /tmp/ -name llmfit -type f to locate binary after tar extraction
    - Main section contains soft_step llmfit_install install_llmfit after step firewall configure_firewall
    - bash -n auto-vllm/setup.sh exits 0 (valid syntax)
  </acceptance_criteria>
  <verify>
    <automated>bash -n auto-vllm/setup.sh && grep -q 'soft_step llmfit_install install_llmfit' auto-vllm/setup.sh && grep -q 'LLMFIT_VERSION' auto-vllm/setup.sh && ! grep -q 'exit 1' <(grep -A5 'soft_step()' auto-vllm/setup.sh)</automated>
  </verify>
  <done>setup.sh has soft_step wrapper, install_llmfit function, env-var defaults, and soft_step invocation in main section. Syntax valid. step() unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add LLMFIT_INSTALL to ProvisioningStep enum and update tests</name>
  <files>inference_proxy/provisioning/state.py, tests/provisioning/test_state.py</files>
  <read_first>
    - inference_proxy/provisioning/state.py (ProvisioningStep enum — FIREWALL at line 29, STARTING_VLLM at line 30)
    - tests/provisioning/test_state.py (test_member_count asserts 18 at line 25, test_member_values expected dict at lines 28-47)
  </read_first>
  <action>
Per D-01, add LLMFIT_INSTALL = "llmfit_install" to ProvisioningStep enum between FIREWALL and STARTING_VLLM. This placement matches execution order in setup.sh where soft_step llmfit_install runs after step firewall.

Update tests/provisioning/test_state.py:
- test_member_count: change assert from 18 to 19
- test_member_values: add "LLMFIT_INSTALL": "llmfit_install" to the expected dict, placed between FIREWALL and STARTING_VLLM entries for consistency

Per D-03, no changes to provisioner.py or its STEP_PATTERN regex. WARN markers are not parsed — they flow through as plain text in LogBuffer.
  </action>
  <acceptance_criteria>
    - state.py contains LLMFIT_INSTALL = "llmfit_install" between FIREWALL and STARTING_VLLM members
    - ProvisioningStep("llmfit_install") returns ProvisioningStep.LLMFIT_INSTALL (enum lookup works)
    - test_state.py test_member_count asserts len(ProvisioningStep) == 19
    - test_state.py test_member_values expected dict includes "LLMFIT_INSTALL": "llmfit_install"
    - uv run pytest tests/provisioning/test_state.py -x passes
    - provisioner.py is NOT modified
  </acceptance_criteria>
  <verify>
    <automated>cd /home/grafuls/Sources/inference-proxy && uv run pytest tests/provisioning/test_state.py -x</automated>
  </verify>
  <done>ProvisioningStep has 19 members including LLMFIT_INSTALL. All test_state.py tests pass. Provisioner unchanged per D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway -> Target Server (SSH) | Setup.sh runs on remote server via SSH provisioning |
| Target Server -> GitHub | setup.sh downloads llmfit binary from external URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | llmfit binary download | accept | Pinned version in LLMFIT_VERSION, LLMFIT_URL overridable for internal mirrors. HTTPS download. Full supply chain verification deferred — llmfit is open-source Rust binary on GitHub. Risk: internal network only, non-security-critical tool (model sizing). |
| T-26-02 | Denial of Service | install_llmfit() | mitigate | soft_step() ensures download failure does not block provisioning (INST-02). wget timeout defaults prevent hang. |
</threat_model>

<verification>
1. bash -n auto-vllm/setup.sh — syntax valid
2. grep confirms soft_step, install_llmfit, LLMFIT_VERSION all present in setup.sh
3. uv run pytest tests/provisioning/test_state.py -x — all pass with 19-member enum
4. uv run pytest tests/ -x — full suite green (no regressions)
</verification>

<success_criteria>
- setup.sh installs llmfit via idempotent install_llmfit() wrapped in soft_step()
- Failure emits WARN marker and continues (does not exit 1)
- ProvisioningStep.LLMFIT_INSTALL exists with value "llmfit_install"
- All existing tests pass, updated tests pass
</success_criteria>

<output>
Create `.planning/phases/26-llmfit-installation/26-01-SUMMARY.md` when done
</output>
