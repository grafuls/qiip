# Milestone v1.7: HuggingFace Integration

**Status:** SHIPPED 2026-07-29
**Phases:** 30-32
**Total Plans:** 5

## Overview

Download models from HuggingFace Hub to NFS storage, integrated with llmfit recommendations in the dashboard. Operators can configure HF tokens for gated models, browse the NFS model catalog, trigger downloads, and monitor status directly from the node detail page.

## Phases

### Phase 30: Foundation & Model Catalog

**Goal**: Gateway can discover which models are already downloaded on NFS storage
**Depends on**: Nothing (first phase of v1.7)
**Plans**: 2 plans

Plans:

- [x] 30-01: HuggingFace settings, catalog service, and unit tests
- [x] 30-02: Admin endpoint wiring, lifespan startup, integration tests

**Details:**
- HuggingFaceSettings sub-model with required cache_dir and optional SecretStr api_token
- ModelCatalogService scanning HF cache via asyncio.to_thread, filtering to model repos
- GET /admin/models/catalog endpoint wired via dependency injection
- HF startup guards (XET disable, progress bars off) in lifespan
- cache_dir fail-fast validation at startup

### Phase 31: Download Service & API

**Goal**: Operators can download models from HuggingFace Hub to NFS and monitor download status
**Depends on**: Phase 30
**Plans**: 2 plans

Plans:

- [x] 31-01: DownloadService, Pydantic models, and unit tests
- [x] 31-02: DI provider, admin endpoints, lifespan wiring, integration tests

**Details:**
- DownloadService with thread-safe status tracking, semaphore-gated concurrency (2-3 workers)
- Background snapshot_download via asyncio.to_thread with task-set GC prevention
- POST /admin/models/download (202 Accepted) and GET /admin/models/downloads
- GatedRepoError handling for access-restricted models

### Phase 32: Dashboard Download Integration

**Goal**: Operators can trigger and monitor model downloads directly from the recommendations table
**Depends on**: Phase 31
**Plans**: 1 plan

Plans:

- [x] 32-01: Download column, catalog cross-reference, download trigger, status polling

**Details:**
- renderDownloadCell: 4-state renderer (downloading/complete+catalog/failed/default)
- Parallel fetch of catalog and downloads alongside recommendations with independent error handling
- Optimistic UI: immediate badge swap on download trigger
- Lazy 4s polling with single-timer guard, auto-stop when no active downloads
- Module-level catalogSetCache shared between initial load and poll updater

**UI hint**: yes

---

## Milestone Summary

**Key Decisions:**

- Single new dependency: huggingface-hub >=1.25, <2.0
- Must use cache_dir= (not local_dir=) for HF cache layout compatibility with vLLM
- Downloads are sync — dedicated ThreadPoolExecutor (2-3 workers)
- disable_progress_bars() at startup for thread safety
- HF_HUB_DISABLE_XET=1 env var to avoid hang issues
- llmfit model name IS the HF repo_id — zero mapping needed
- GatedRepoError derives from RepositoryNotFoundError (exception ordering matters)
- Fetch catalog and downloads as independent requests with try/catch, not Promise.all

**Issues Resolved:**

- Module-level Settings() import-time breakage with required HF fields (conftest.py env var fix)
- GatedRepoError constructor requires httpx.Response kwarg (mock response in tests)
- XSS vectors and catalog cache bypass in node_detail.js (fix commit a332b6a)

**Issues Deferred:**

- Token validation on startup (DLE-01)
- Model size/file count in catalog entries (DLE-02)
- Pre-flight auth check for gated models (DLE-03)
- Download resumption across restarts (DLE-05)

**Technical Debt Incurred:**

- In-memory download status tracking (lost on gateway restart)
- No granular download progress (percentage/bytes) — simple status only

---

_For current project status, see .planning/ROADMAP.md_
